# PROJECT_RULES.md

## 目的
このドキュメントは、Godot 4.4.1 stable / GDScript で大規模ゲームを段階的に開発するための運用ルールを定義する。

## 開発原則
- 小さな差分で進める（1タスク=1意図が分かる最小変更）。
- 既存ノード名・ファイル名は、明示的な合意なしに変更しない。
- シーン、ゲームロジック、UI、データ定義を分離する。
- Godot 4.x のAPIを使用し、古い記法を持ち込まない。
- 各作業後に「変更ファイル」「動作確認手順」「TODO」を報告する。

## 変更ポリシー
- 破壊的変更（命名変更、ディレクトリ移動、シグネチャ変更）は事前提案→合意後に実施。
- 1つのPR/コミットには1つの目的を持たせる。
- 実装前に対象範囲を明記し、範囲外の修正は原則しない。

## ファイル/責務分離（雛形）
- `scenes/`: ノード構造とシーン遷移
- `scripts/gameplay/`: ゲームルール、状態遷移、ドメインロジック
- `scripts/ui/`: UI表示、入力ハンドリング、UI状態
- `data/`: 定数、設定、マスターデータ定義（Resource/JSON等）

## コーディング規約（雛形）
- GDScript 2.0（Godot 4.x）準拠。
- 可能な箇所は型注釈を付ける。
- マジックナンバーは定数化する。
- 依存方向は `UI -> gameplay` 参照を許可し、逆依存を避ける。

## タスク完了時レポート（テンプレート）
- 変更ファイル:
  - `path/to/file`
- 動作確認手順:
  1. Godotでプロジェクトを開く
  2. 対象シーンを実行
  3. 期待結果を確認
- TODO:
  - [ ] 次に着手する小タスク

## 非目標（今回）
- ゲーム本体の具体実装は行わない。
