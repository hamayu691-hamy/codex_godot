# Pinball Battle 仕様書（2026-05-10時点）

## 1. 目的
本仕様書は、現在の実装状態を整理し、**生成AIと今後の開発方針を相談するための共通土台**として利用する。

---

## 2. 現在のゲーム概要
- ジャンル: 1画面型のピンボールバトル（アクション）
- プレイヤー操作:
  - 左フリッパー操作（`move_left`）
  - 右フリッパー操作（`move_right`）
  - ボール初速付与（`launch_ball`）
- 勝利条件:
  - 敵HPを0以下にする
- 敗北条件:
  - 自機ボールHPが0以下になる
- ステージ:
  - 固定1ステージ（壁・バンパーをランタイム生成）

---

## 3. 実装済みシステム（事実ベース）

### 3.1 フリッパー制御
- 左右フリッパーは「待機角」と「アクティブ角」を持ち、入力に応じて補間回転する。
- フリッパー角速度を算出し、一定しきい値を超える打撃時のみ強い反発を与える。

### 3.2 ボール挙動
- `RigidBody2D` の速度に最大値（クランプ）を適用。
- 発射時に初期インパルスを付与。
- ドレイン侵入時にボール位置・速度をリセット。

### 3.3 HP/ダメージ
- 敵HP初期値: 20
- ボールHP初期値: 20
- ボール上部にHPバー表示。
- 敵HPラベル更新、勝利/敗北ラベル表示。
- ダメージポップアップ演出（上昇+時間消去）。

### 3.4 バンパー
- データ定義（位置/ダメージ/反発/タイプ）から動的生成。
- 衝突判定は円形。
- 見た目は12頂点ポリゴンで生成。

### 3.5 壁
- データ定義（名前/位置/サイズ/回転/色/物理マテリアル）から動的生成。
- 側壁はフリッパー側に傾斜。
- フリッパー周辺にガイド壁を配置。

### 3.6 敵攻撃（弾）
- 一定間隔で敵ガン位置から弾を発射。
- 弾速度・弾ダメージ・当たり判定半径を定数管理。
- 弾がボールにヒットした際にボールHPを減少。

---

## 4. 主要パラメータ（現状値）
- 敵HP: `20`
- ボールHP: `20`
- ボール最大速度: `1350.0`
- 発射インパルス: `(120.0, -750.0)`
- フリッパー打撃インパルス: `1600.0`
- フリッパー有効角速度しきい値: `3.0 rad/s`
- 弾速度: `420.0`
- 弾ダメージ: `2`
- 弾発射間隔: `1.1s`

> 備考: 値はプレイフィールに強く影響するため、将来的にData層へ抽出候補。

---

## 5. 既知の設計方針
- シーン構造は単純化し、ロジックは `scripts/gameplay/pinball_battle_scene.gd` に集約。
- バンパーと壁はハードコード配置ではなく、配列定義からの生成に統一。
- UIはゲーム状態表示（HP/勝敗）を中心に最小構成。

---

## 6. いま未解決の論点（生成AI相談向け）
1. **バランス調整方針**
   - 被弾頻度、敵弾間隔、反発強度の適正値。
2. **責務分離**
   - 1ファイル集中ロジックを分割する適切な単位（例: `BallSystem`, `EnemySystem`, `StageBuilder`）。
3. **データ外部化**
   - `wall_configs` / `bumper_configs` / バトル定数を `.tres` / `.json` / `.gd` リソースどれで管理するか。
4. **テスト戦略**
   - Godot環境での最小回帰チェック項目（手動 + 自動化可能範囲）。
5. **ゲーム性拡張**
   - ステージ差分、バンパー特性差、スコア導入、複数敵などの優先順位。

---

## 7. 生成AIと進める推奨プロセス

### Phase 1: 仕様の固定（このドキュメントを起点）
- 目的: 現状の「事実」と「今後の意思決定」を分離する。
- 進め方:
  1. 本仕様書の「実装済みシステム」を毎PRで更新。
  2. 「未解決の論点」に対し、1PRにつき1論点のみ解く。

### Phase 2: 分割実装計画の作成
- 目的: 巨大差分を防ぐ。
- 進め方:
  - 生成AIへ以下テンプレートで依頼する:
    - タスク名
    - 目的
    - 変更対象ファイル
    - 受け入れ条件（画面上で確認できること）
    - ロールバック容易性

### Phase 3: バランス調整ループ
- 目的: 体感品質向上。
- 進め方:
  - 1回で触る数値は最大3個まで。
  - 毎回「変更前→変更後→体感メモ」を残す。

### Phase 4: ドキュメント駆動開発へ移行
- 目的: 仕様の再利用性確保。
- 進め方:
  - `GAME_DESIGN.md` を雛形から実体化。
  - `ROADMAP.md` のチェックボックスを実態に合わせ更新。

---

## 8. 生成AIへの相談テンプレート（コピペ用）

### A. 設計相談
「以下の現行仕様で、責務分離のリファクタリング案を3案ください。各案について、
1) 変更ファイル、2) メリット、3) リスク、4) 最小導入手順 を示してください。」

### B. バランス相談
「現在値: 敵HP20 / 自機HP20 / 弾間隔1.1秒 / 弾ダメージ2。
平均プレイ時間90秒を目標にした場合の調整案を、保守的・標準・攻撃的の3パターンでください。」

### C. 実装依頼
「次の差分だけ実装してください。
- 対象:
- 変更ファイル:
- 受け入れ条件:
- 非対象（やらないこと）:
実装後、変更点要約とテスト手順を提示してください。」

---

## 9. 直近推奨TODO
- [ ] ロジック分割の最初の1歩として `StageBuilder` 抽出。
- [ ] バトル定数の一部を外部データ化（まずは弾関連）。
- [ ] プレイテストログ様式（Markdown）を追加。
- [ ] 勝利/敗北後の入力制御と再開導線を明確化。

